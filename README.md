<div align="center">
  <h1>Art Supabase WMS</h1>
  <p><strong>面向仓网、库存与入出库执行的仓储管理应用</strong></p>
  <p>以库存身份和作业任务为主线，规划预约、收货、上架、拣选、复核、发运与库存追溯。</p>

  <p>
    <a href="https://gitee.com/wangyanghub/art-supabase-wms">Gitee</a>
    ·
    <a href="https://github.com/869123771/art-supabase-wms">GitHub</a>
    ·
    <a href="https://gitee.com/wangyanghub/art-supabase-pro">主平台</a>
    ·
    <a href="https://869123771.github.io/art-supabase-doc/modules/wms">使用文档</a>
    ·
    <a href="./README.en.md">English</a>
  </p>
</div>

## 项目定位

Art Supabase WMS 是 Art Supabase Pro 的仓储管理业务应用，目标是围绕仓库、库区、库位、物料、批次/序列号和库存状态，连接入库、库内、出库与盘点调整，形成账实一致、任务可追踪的仓储作业闭环。

本仓只维护 WMS 页面、业务 API、领域类型与仓储执行规则。认证、租户、菜单、权限、布局、路由、公共组件、Store 和 Supabase 公共客户端由 [`art-supabase-pro`](https://gitee.com/wangyanghub/art-supabase-pro) 统一提供；仓库、库位和物料的统一身份由 MDM 及其权威来源提供。

## 当前状态

WMS 当前处于业务实施准备阶段，已经完成独立应用骨架、平台运行时、动态菜单、角色访问权限和仓储运营工作台。库存台账、入库、库内、出库和盘点执行仍属于后续建设范围，仓库不会用演示数据冒充真实库存。

| 建设领域 | 当前状态 | 目标能力                                             |
| -------- | -------- | ---------------------------------------------------- |
| 应用底座 | 已完成   | 独立运行、平台装载、统一主题导航与角色访问           |
| 仓网基础 | 待接入   | 仓库、库区、库位、容器、物料和存储约束               |
| 库存台账 | 规划中   | 库存身份、批次/序列号、状态、占用、冻结与可用量      |
| 作业执行 | 规划中   | 预约、收货、上架、补货、移库、拣选、复核、发运与退货 |
| 运营优化 | 规划中   | 盘点差异、库存预警、周转、库容、作业效率与策略优化   |

## 目标作业闭环

```text
到货预约
  → 收货与数量交接
  → 上架与库存形成
  → 补货 / 移库 / 盘点
  → 波次 / 拣选 / 复核
  → 发运与装车交接
  → 库存流水、批次与任务追溯
```

## 实施路线

1. 先通过 MDM 确定仓库、库区、库位、物料及其权威来源和映射关系。
2. 建立库存身份与不可变流水，区分现存量、可用量、占用、冻结和在途。
3. 按单据和任务实现入库、库内、出库、退货与盘点，不直接改库存余额。
4. 贯通 MES 领料/退料/完工入库以及 TMS 到货/发运交接的稳定服务契约。
5. 在真实作业和库存流水基础上建设预警、周转、库容与效率分析。

## 独立运行

环境要求：Node.js `>= 22.0.0`、pnpm `>= 11.9.0`。

```powershell
pnpm install
pnpm dev
```

默认访问 `http://localhost:3018`。

```powershell
pnpm check
pnpm build
pnpm preview
```

生产构建输出到 `docs/`，默认公共路径为 `/art-supabase-wms/`，可作为 Pages 发布目录。

## 与主仓协作

WMS 业务修改在本仓提交并推送，随后在主仓更新 `modules/art-supabase-wms` 子模块指针。跨模块数据通过用途明确、字段最小化、租户隔离的 API/RPC 契约读取；本仓不直接导入 MDM、MES、TMS 或其他业务仓源码。

## 安全原则

- 普通用户只能访问当前租户及授权仓库范围内的数据。
- 收货、上架、移库、冻结、解冻、拣选、复核、发运和盘点调整必须拆分权限并由服务端校验。
- 库存变化必须由业务单据和不可变流水驱动，不能通过前端直接修改余额。
- 页面可见不代表后端授权，数据库 RLS、RPC、库存约束与幂等规则是最终安全边界。

## 许可证

本项目采用 [MulanPSL-2.0](LICENSE) 许可证。
